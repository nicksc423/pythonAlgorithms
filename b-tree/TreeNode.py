class TreeNode(object) :
        def __init__(self,val):
            self.val = val
            self.left = None # the pointer initially points to nothing
            self.right = None # the pointer initially points to nothing
